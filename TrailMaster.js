var TrailMaster = function () {
    const version = '0.0.1';
    var settings = {
        tokenCharMgrEnabled: true,
      };
    log(`-=> TrailMaster v${version} <=-`);
  };

on('ready', function () {
    'use strict';

    var TM = new TrailMaster();
  });

